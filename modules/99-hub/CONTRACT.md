# 接口契约状态（99-hub）

> 目的：给各模块一个“当前契约推进到哪了”的单点入口，减少口头对齐成本。

## 1) 契约形式（已选择：OpenAPI 3.1）

- 决定：采用 OpenAPI 3.1（YAML/JSON）作为接口契约单一来源
- 说明：数据模型建议复用 JSON Schema（OAS 3.1 原生支持），便于前后端/Mock/校验工具共用
- 待落地（路径/流程）：
  - 契约建议存放在仓库单独目录（避免分散到各模块）
  - miniapp mock 与后端路由建议从同一份 OpenAPI 生成/校验

## 2) 下一步

- 在 99-hub 汇总 `modules/99-hub/REQUESTS.md` 的跨模块请求，产出第一版 OpenAPI 文件目录与接口清单
