<view class="page">
  <view class="tip" wx:if="{{tip}}">{{tip}}</view>

  <view class="mock">
    <view class="mock-title">播放页（Mock）</view>
    <view class="mock-desc">此页用于演示“进入播放页 + 记录进度（storage）”的后续里程碑。</view>
    <view class="mock-meta">课程ID：{{id}}</view>
    <view class="mock-meta">当前进度：{{currentSeconds}}s</view>
    <view class="mock-meta">学习状态：{{learned ? '已学' : '未学'}}（本地）</view>
  </view>

  <view class="learned">
    <button class="btn ghost" bindtap="toggleLearned">{{learned ? '标记为未学' : '标记为已学'}}</button>
  </view>

  <view class="paywall" wx:if="{{!svipBound}}">
    <view class="paywall-title">SVIP 未开通（占位）</view>
    <view class="paywall-desc">部分课程/功能需 SVIP，当前仅演示文案与入口（后续接后端）。</view>
    <view class="paywall-actions">
      <button class="btn ghost" bindtap="goBindSvip">去绑定 SVIP</button>
    </view>
  </view>

  <view class="controls">
    <button class="btn primary" bindtap="addTenSeconds">模拟播放 +10s</button>
  </view>

  <view class="slider">
    <slider min="0" max="600" step="1" value="{{currentSeconds}}" bindchange="onSliderChange"></slider>
    <view class="slider-tip">拖动滑块模拟播放进度（0~600s）</view>
  </view>
</view>
