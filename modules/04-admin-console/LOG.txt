--- 2026-01-20 ---
分支：`feat/04-admin-console-auth-skeleton`

变更点：
- 新增管理台 Web 最小骨架（Vite + React + TS），包含登录页占位、AuthProvider、本地 token 持久化、权限守卫（`admin:access`）。
- 更新 `TASKS.md`：完成“登录与权限（管理员）”。
- 更新文档：在模块 README 补充 `web/` 入口说明。

验证日志：
- `node -v` → `v20.19.5`
- `npm -v` → `10.8.2`
- `cd modules/04-admin-console/web && npm install`（成功）
- `npm run build`（成功，生成 `dist/`，未提交）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174`，点击“开发登录”进入首页；退出后会回到登录页。

--- 2026-01-20 ---
分支：`feat/04-admin-console-question-bank-codex`

变更点：
- 题库管理最小骨架：科目/题库包/章节/题目列表（本地示例数据 + 占位 CRUD，存储于 localStorage）。
- 路由：`/question-bank` 从占位页替换为可交互页面。
- 更新 `TASKS.md`：完成“题库管理：科目/题库包/章节/题目列表”。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“题库管理（占位）”
  - 可新增/重命名/删除：科目、题库包、章节、题目
  - 点击“重置示例数据”可回到初始数据

--- 2026-01-20 ---
分支：`feat/04-admin-console-imports-codex`

变更点：
- 导入管理最小骨架：上传占位、任务列表、任务详情、错误明细、错误报告下载（CSV 占位）。
- 路由：`/imports` 与 `/imports/:taskId` 从占位页替换为可交互页面。
- 更新 `TASKS.md`：完成“导入管理：上传、任务列表、任务详情、错误报告下载”。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 进入“导入管理”
  - 点击“上传（占位）”创建任务（会模拟进度并完成）
  - 点击“详情”进入任务详情页
  - 有错误的任务可点击“下载错误报告（占位）”得到 CSV
