--- 2026-01-20 ---
分支：`feat/04-admin-console-auth-skeleton`

变更点：
- 新增管理台 Web 最小骨架（Vite + React + TS），包含登录页占位、AuthProvider、本地 token 持久化、权限守卫（`admin:access`）。
- 更新 `TASKS.md`：完成“登录与权限（管理员）”。
- 更新文档：在模块 README 补充 `web/` 入口说明。

验证日志：
- `node -v` → `v20.19.5`
- `npm -v` → `10.8.2`
- `cd modules/04-admin-console/web && npm install`（成功）
- `npm run build`（成功，生成 `dist/`，未提交）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174`，点击“开发登录”进入首页；退出后会回到登录页。

--- 2026-01-20 ---
分支：`feat/04-admin-console-question-bank-codex`

变更点：
- 题库管理最小骨架：科目/题库包/章节/题目列表（本地示例数据 + 占位 CRUD，存储于 localStorage）。
- 路由：`/question-bank` 从占位页替换为可交互页面。
- 更新 `TASKS.md`：完成“题库管理：科目/题库包/章节/题目列表”。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“题库管理（占位）”
  - 可新增/重命名/删除：科目、题库包、章节、题目
  - 点击“重置示例数据”可回到初始数据

--- 2026-01-20 ---
分支：`feat/04-admin-console-imports-codex`

变更点：
- 导入管理最小骨架：上传占位、任务列表、任务详情、错误明细、错误报告下载（CSV 占位）。
- 路由：`/imports` 与 `/imports/:taskId` 从占位页替换为可交互页面。
- 更新 `TASKS.md`：完成“导入管理：上传、任务列表、任务详情、错误报告下载”。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 进入“导入管理”
  - 点击“上传（占位）”创建任务（会模拟进度并完成）
  - 点击“详情”进入任务详情页
  - 有错误的任务可点击“下载错误报告（占位）”得到 CSV

--- 2026-01-20 ---
分支：`feat/04-admin-console-announcements-codex`

变更点：
- 公告管理最小骨架：新增/编辑/删除、草稿/发布切换、搜索（本地示例数据 + localStorage）。
- 路由：新增 `/cms`（资讯/公告管理）。
- 更新权限占位：开发登录默认包含 `cms:read` / `cms:write`。
- 更新 `TASKS.md`：完成“资讯/公告管理”。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“公告管理”
  - 可新增公告、编辑标题/内容
  - 草稿可“发布”，已发布可“下线”
  - 支持搜索标题/内容、删除、重置示例数据

--- 2026-01-21 ---
分支：`feat/04-admin-console-dashboard-v1`

变更点：
- 新增数据看板最小骨架：活跃/正确率/完成率指标卡片 + 近 7/14/30 天趋势表格（占位数据）。
- 首页增加入口，并补齐 dev 登录默认权限 `dashboard:read`。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“数据看板（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-plans-v1`

变更点：
- 新增套餐管理最小骨架：套餐列表 + 上下线/改价/改有效期 + 权益条目维护（占位 localStorage）。
- 首页增加入口，并补齐 dev 登录默认权限 `plan:read` / `plan:write`。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“套餐管理（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-news-v1`

变更点：
- 公告管理补齐“置顶 + 详情”：列表支持置顶优先排序，新增 `/cms/:id` 详情页。
- 修复 cms store 占位更新不刷新的问题（改为 React state + localStorage 持久化）。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“公告管理”
  - 可置顶/取消置顶；点击“详情”进入详情页操作发布/下线/编辑/删除

--- 2026-01-21 ---
分支：`feat/04-admin-console-home-slots-v1`

变更点：
- 新增首页运营位配置最小骨架：banner + 推荐题库/课程/套餐（占位 localStorage）。
- 支持导出 `home-slots-*.json` 作为小程序首页“配置驱动”的 Mock 数据。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“首页运营位配置（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-orders-v1`

变更点：
- 新增订单管理最小骨架：订单列表 + 详情（占位 localStorage）。
- 预留发票申请流程：提供 invoiceStatus 状态位与占位操作按钮。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“订单管理（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-banners-v2`

变更点：
- 新增轮播图管理最小骨架：banner 列表 + 排序 + 跳转链接 + 开关 + 间隔秒（占位 localStorage，图片用 URL 代替上传）。
- 首页增加入口，并补齐 dev 登录默认权限 `banner:read` / `banner:write`。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“轮播图管理（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-courses-v2`

变更点：
- 新增课程视频管理最小骨架：按学段/年级/科目/单元关联，维护封面/标题/简介/时长（占位 localStorage）。
- 首页增加入口，并补齐 dev 登录默认权限 `course:read` / `course:write`。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“课程视频管理（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-question-items-v2`

变更点：
- 新增试题管理最小骨架：按学段/科目/单元/题型归档 + CSV 批量导入/导出（占位 localStorage）。
- 首页增加入口，并补齐 dev 登录默认权限 `question:read` / `question:write`。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“试题管理（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-svip-codes-v4`

变更点：
- SVIP 码补齐绑定记录字段 `redeemedBy`，并支持“按码绑定（占位）”将 ACTIVE 码标记为 REDEEMED。
- CSV 导出增加 `redeemedBy` 列，并修复 svip store 不刷新的问题（React state + 持久化）。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“SVIP 码管理（占位）”

--- 2026-01-21 ---
分支：`feat/04-admin-console-ops-export-v2`

变更点：
- 扩展 `/exports-ops` 的 `ops-config.json`：从公告与本地配置汇总 news/recommendations/plans/courses 占位数据。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 点击“运营配置导出（占位）”并下载 ops-config.json

--- 2026-01-21 ---
分支：`feat/04-admin-console-home-permissions-v1`

变更点：
- 首页入口按权限细分显示（无权限的入口隐藏），避免仅依赖 admin:access 全量展示。

验证日志：
- `cd modules/04-admin-console/web && npm run build`（成功）

验收方式（本地）：
- `cd modules/04-admin-console/web`
- `npm install`
- `npm run dev`
- 打开 `http://localhost:5174` → 开发登录 → 首页检查入口按权限展示
