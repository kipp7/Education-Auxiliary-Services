export type Course = {
  id: string;
  stage: string;
  grade: string;
  subject: string;
  unit: string;
  coverUrl: string;
  title: string;
  description: string;
  durationSeconds: number;
  createdAt: string;
  updatedAt: string;
};

