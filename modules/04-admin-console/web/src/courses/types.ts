export type CourseVideo = {
  id: string;
  stage: string;
  grade: string;
  subject: string;
  unit: string;
  title: string;
  coverUrl: string;
  intro: string;
  durationSeconds: number;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
};

